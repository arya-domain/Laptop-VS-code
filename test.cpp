#include <bits/stdc++.h>
using namespace std;

bool palindrome(string s) 
{   

    return 1;
}
int main ()
{
    vector<int> v  = {1,2,1,3,5};
    map<int,int> m;
    for (auto i : v)
         m[i]++;
    for (auto i : m) 
    cout << i.first  << " " << i.second << endl; 

    return 0;
}
